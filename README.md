# tree-sitter-tono

tree-sitter grammar for `.tono` - highlighting and incremental parsing in editors.
It is a second grammar (highlighting only), kept in sync with the frontend's hand-written parser.
